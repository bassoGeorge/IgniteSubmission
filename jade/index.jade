doctype html
html(ng-app='app')
  head
    meta(charset='utf-8')
    link(href='css/style.css' rel='stylesheet')
    script(src='js/completeApp.min.js')

  body
    .grid-frame.vertical
      nav.primary.title-bar(ng-controller='TitleCtrl')
        .center.title IMDB Web app
        span.left(ng-show='showBackButton()')
          a(ui-sref='home') Back
      main.grid-block(ui-view)
